# torch >= 2.9.0 has an MPS allocator regression that balloons driver memory
# to the watermark cap (~27 GiB on a 32 GB Mac) on the first OmniVoice forward
# pass, independent of input length. Bisected: 2.8 OK, 2.9/2.10/2.11 OOM.
# Retest with `dist-python/bin/python3.11 -c "..."` against a real paragraph
# before bumping. torchcodec is intentionally absent — it only sits on
# torchaudio>=2.10's save() dispatch, bypassed via soundfile.write.
torch==2.8.0
torchaudio==2.8.0
transformers==5.4.0
numpy==2.4.4
safetensors==0.7.0
accelerate==1.13.0

# App deps
omnivoice==0.2.1
fastapi==0.135.2
uvicorn[standard]==0.42.0
python-multipart==0.0.22
pydantic-settings==2.13.1
pytest==9.0.2
uroman==1.3.1.1
